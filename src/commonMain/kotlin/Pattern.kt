import Number.*
import com.soywiz.korge.view.*
import com.soywiz.korim.color.*

enum class Pattern() {
    TRIPLE,
    SQUARE,
    LBLOCK,
    SQUIGGLYBLOCK,
    TBLOCK;
}

fun checkForPattern(list: List<Position>): Boolean {
    return true
}